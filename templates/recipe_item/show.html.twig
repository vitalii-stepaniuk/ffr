{% extends 'base.html.twig' %}

{% block title %}RecipeItem{% endblock %}

{% block body %}
    <h1>RecipeItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recipe_item.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ recipe_item.quantity }}</td>
            </tr>
            <tr>
                <th>Weight</th>
                <td>{{ recipe_item.weight }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('recipe_item_index') }}">back to list</a>

    <a href="{{ path('recipe_item_edit', {'id': recipe_item.id}) }}">edit</a>

    {{ include('recipe_item/_delete_form.html.twig') }}
{% endblock %}

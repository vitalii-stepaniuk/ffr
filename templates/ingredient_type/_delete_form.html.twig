<form method="post" action="{{ path('ingredient_type_delete', {'id': ingredient_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ingredient_type.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}WaterTemperatureCondition{% endblock %}

{% block body %}
    <h1>WaterTemperatureCondition</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ water_temperature_condition.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ water_temperature_condition.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('water_temperature_condition_index') }}">back to list</a>

    <a href="{{ path('water_temperature_condition_edit', {'id': water_temperature_condition.id}) }}">edit</a>

    {{ include('water_temperature_condition/_delete_form.html.twig') }}
{% endblock %}

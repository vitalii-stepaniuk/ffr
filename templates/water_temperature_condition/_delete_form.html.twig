<form method="post" action="{{ path('water_temperature_condition_delete', {'id': water_temperature_condition.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ water_temperature_condition.id) }}">
    <button class="btn">Delete</button>
</form>

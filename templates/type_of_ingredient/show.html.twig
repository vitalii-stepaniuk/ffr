{% extends 'base.html.twig' %}

{% block title %}TypeOfIngredient{% endblock %}

{% block body %}
    <h1>TypeOfIngredient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_of_ingredient.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ type_of_ingredient.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_of_ingredient_index') }}">back to list</a>

    <a href="{{ path('type_of_ingredient_edit', {'id': type_of_ingredient.id}) }}">edit</a>

    {{ include('type_of_ingredient/_delete_form.html.twig') }}
{% endblock %}
